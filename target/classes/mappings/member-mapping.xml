<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberDAO">


	<select id="getStudent" resultType="studentDTO"> SELECT * FROM STUDENT WHERE ID = #{id} </select>

	<select id="getAdmin" resultType="professorDTO"> SELECT * FROM PROFESSOR WHERE ID = #{id} </select>



</mapper>