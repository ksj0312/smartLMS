<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardDAO">

	<select id="getBoardList" resultType="BoardDTO">
		SELECT * FROM BOARD
	</select>
	
	<insert id="insertBoard">
      INSERT INTO BOARD(B_TARGET, B_TITLE, B_INFO, B_WRITER, B_TYPE) VALUES(#{b_target}, #{b_title}, #{b_info}, #{b_writer}, #{b_type})
   </insert>
	
	<select id="getBoard" resultType="BoardDTO">
		SELECT * FROM BOARD WHERE B_NUMBER = #{b_number}
	</select>

	<delete id="deleteBoard">
      DELETE FROM BOARD WHERE B_NUMBER = #{b_number}
   </delete>
   
   <update id="updateBoard">
      UPDATE BOARD SET B_TITLE = #{b_title}, B_INFO = #{b_info} WHERE B_NUMBER = #{b_number}
   </update>

</mapper>